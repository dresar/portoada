# Core dependencies
Django==4.2.7
Pillow==10.1.0
python-dotenv==1.0.0

# Database
psycopg2-binary==2.9.9  # For PostgreSQL

# Development & Debugging
django-debug-toolbar==4.2.0
django-extensions==3.2.3

# Deployment
gunicorn==21.2.0
whitenoise==6.5.0  # For serving static files

# Security
django-cors-headers==4.3.0

# Image processing
pillow==10.1.0

# Utilities
markdown==3.5.1  # For rendering markdown content
python-slugify==8.0.1  # For generating slugs

# Frontend integration
django-compressor==4.4  # For compressing CSS/JS